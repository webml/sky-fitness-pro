import React from "react";

import * as S from './styles'

const FormInput = ({type, placeholder}) => {
    return(
        <S.FormInput type={type} placeholder={placeholder} />
    )
}

export default FormInput